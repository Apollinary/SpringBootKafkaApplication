apiVersion: v1
kind: Service
metadata:
  name: {{ include "kafka-project.fullname" . }}
  labels:
    {{- include "kafka-project.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kafka-project.selectorLabels" . | nindent 4 }}
